---
import { socials } from '../data/site';
import { SOCIAL_ICON_PATHS } from '../data/icons';

/**
 * Riel del margen izquierdo: un filete vertical y, en el mismo eje, los enlaces
 * sociales.
 *
 * Va fijo a la ventana, así que acompaña al scroll igual que la firma del
 * encabezado. Antes no podía: la sección de FlowTrix era una banda azul marino
 * y estos iconos en tinta se habrían perdido encima. Ahora la página es clara
 * de arriba abajo, y el bloque azul del pie queda muy a la derecha del riel.
 */
---

{/* Solo a partir de xl: por debajo de ~1280 px el riel se metería dentro del
    contenedor de 1180 px y chocaría con el texto. */}
<div class="fixed top-0 left-6 z-30 hidden flex-col items-center pt-16 xl:flex">
  {/* Arranca justo bajo la barra del encabezado, no debajo de ella. */}
  <span aria-hidden="true" class="h-44 w-px bg-ink/25"></span>

  <ul class="mt-7 flex flex-col items-center gap-6">
    {
      socials.map((social) => (
        <li>
          <a
            href={social.href}
            aria-label={social.kind}
            rel="me noopener"
            target="_blank"
            class="block text-ink/55 transition-colors duration-200 hover:text-link"
          >
            <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" class="size-6">
              {SOCIAL_ICON_PATHS[social.kind.toLowerCase()].map((d) => (
                <path d={d} />
              ))}
            </svg>
          </a>
        </li>
      ))
    }
  </ul>
</div>
